apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  labels:
    app: wordpress
    tier: database
spec:
  selector:
    app: wordpress
    tier: database
  ports:
  - port: 3306
    targetPort: db-port
  type: ClusterIP
