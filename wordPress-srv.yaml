apiVersion: v1
kind: Service
metadata:
  name: WordPress-service
  labels:
    app: WordPress
    type: frontend
spec:
  selector:
    app: WordPress
    type: frontend
  ports:
  - name: http-sv-port
    port: 80
    targetPort: http-port
  - name: https-sv-port
    port: 443
    targetPort: https-port
  type: NodePort
